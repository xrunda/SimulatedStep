version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: stepmaster-admin:latest
    container_name: stepmaster-admin
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

